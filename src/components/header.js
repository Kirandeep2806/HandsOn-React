import "../styles/header.scss";

const Header = () => {
    return (
        <header className="App-header" >
            I am the header
        </header>
    );
}

// Exporting via "named export"
export { Header };
